<div>
    <p>CLUB MESSAGES</p>
    <% messages.forEach(message=>{%>
    <div>
        <p><%= message.message_title %></p>
        <p><%= message.message %></p>
        <p><%= message.created_at %></p>
    </div>
   <% }) %>
</div>